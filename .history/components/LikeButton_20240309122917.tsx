"use client";

import useAuthModal from "@/hooks/useAuthModal";
import { useSessionContext } from "@"
const LikeButton = () => {
    return (
        <div>
            Like Button
        </div>
      );
}

export default LikeButton;
