"use client";

import usePlayer from "@/hooks/UsePlayer";

const Player = () => {
    const player = usePlayer();
    const 

    return (
        <div>
            Player!
        </div>
      );
}

export default Player;

