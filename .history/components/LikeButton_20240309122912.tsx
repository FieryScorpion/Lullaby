"use client";

import useAuthModal from "@/hooks/useAuthModal";
import { useSessionContext}
const LikeButton = () => {
    return (
        <div>
            Like Button
        </div>
      );
}

export default LikeButton;
