"use client";

import useAuthModal from "@/hooks/useAuthModal";
import { useSessionContext } from "@supa"
const LikeButton = () => {
    return (
        <div>
            Like Button
        </div>
      );
}

export default LikeButton;
