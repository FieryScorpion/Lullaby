"use client";
import { useState } from "react";

const ModelProvider = () => {
    const [isMounted, setIsMounted] = useState(false);
 use

    return (
        <>
        Modals!</>
    );

}

export default ModelProvider;
